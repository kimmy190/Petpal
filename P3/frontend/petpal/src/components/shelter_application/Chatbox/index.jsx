import React from 'react';

const Chatbox = () => {
    return (
        <section id="chatbox" className="hidden">
            {/* Chatbox content */}
        </section>
    );
};

export default Chatbox;